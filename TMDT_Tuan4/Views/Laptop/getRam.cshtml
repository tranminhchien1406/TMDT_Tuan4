@model IEnumerable<System.Nullable<int>>
<div id="13">
    @foreach (var item in Model)
    {
        @Html.CheckBox(item.ToString());
        <span>@item.ToString()</span>
        <b>&nbsp;</b>
    }
</div>

